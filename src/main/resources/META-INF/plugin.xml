<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <!-- Unique identifier of the plugin. It should be FQN. It cannot be changed between the plugin versions. -->
    <id>cz.loplex.ij-git-reflog</id>

    <!-- Public plugin name should be written in Title Case.
     Guidelines: https://plugins.jetbrains.com/docs/marketplace/best-practices-for-listing.html#plugin-name -->
    <name>Git Reflog</name>

    <!-- A displayed Vendor name or Organization ID displayed on the Plugins Page. -->
    <vendor email="lopin.git@loplex.cz" url="https://github.com/loplex/">loplex</vendor>

    <!-- Description of the plugin displayed on the Plugin Page and IDE Plugin Manager.
         Guidelines: https://plugins.jetbrains.com/docs/marketplace/best-practices-for-listing.html#plugin-description -->
    <description><![CDATA[
        Adds a <b>Reflog</b> tab to the Git tool window.<br>
        The tab shows the entries of <code>git reflog</code> for the current repository, so that commits that are no
        longer reachable from any branch - the ones left behind by a reset, an amend or a rebase - stay one click away.
        ]]></description>

    <!-- Product and plugin compatibility requirements.
         Read more: https://plugins.jetbrains.com/docs/intellij/plugin-compatibility.html -->
    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.modules.vcs</depends>
    <depends>Git4Idea</depends>

    <resource-bundle>messages.GitReflogBundle</resource-bundle>

    <extensions defaultExtensionNs="com.intellij">
        <!-- Persistent tabs of the Git and Commit tool windows are contributed through this extension point;
             isInCommitToolWindow=false puts the tab next to Log and Console in the Git tool window. -->
        <changesViewContent id="cz.loplex.git.reflog"
                            tabName="Reflog"
                            className="cz.loplex.reflog.ui.GitReflogContentProvider"
                            predicateClassName="cz.loplex.reflog.ui.GitReflogContentVisibilityPredicate"
                            preloaderClassName="cz.loplex.reflog.ui.GitReflogContentPreloader"
                            displayNameSupplierClassName="cz.loplex.reflog.ui.GitReflogDisplayNameSupplier"
                            isInCommitToolWindow="false"/>
    </extensions>

    <actions>
        <group id="GitReflog.Toolbar">
            <!-- First in the row, which puts it against the count of entries read: the two say one thing between
                 them, and clicking it moves the count. Last in the row it instead sat against Refresh, where a
                 button carrying text and no icon reads as a label belonging to the icon beside it. -->
            <action id="GitReflog.LoadMore"
                    class="cz.loplex.reflog.actions.GitReflogLoadMoreAction"/>
            <separator/>
            <action id="GitReflog.ShowFiles"
                    class="cz.loplex.reflog.actions.GitReflogShowFilesAction"
                    icon="AllIcons.Actions.ListFiles"/>
            <!-- The platform's pair of preview buttons: one per side, neither pressed meaning no diff pane. -->
            <action id="GitReflog.PreviewOnTheRight"
                    class="cz.loplex.reflog.actions.GitReflogPreviewOnTheRightAction"
                    icon="AllIcons.Actions.PreviewDetails"/>
            <action id="GitReflog.PreviewAtTheBottom"
                    class="cz.loplex.reflog.actions.GitReflogPreviewAtTheBottomAction"
                    icon="AllIcons.Actions.PreviewDetailsVertically"/>
            <separator/>
            <action id="GitReflog.Refresh"
                    class="cz.loplex.reflog.actions.GitReflogRefreshAction"
                    icon="AllIcons.Actions.Refresh"
                    use-shortcut-of="Refresh"/>
        </group>

        <!-- Toolbar and context menu of the file pane. Show Diff and Group By come from the platform's browser
             itself; these are what the Log adds on top of them. -->
        <group id="GitReflog.ChangesBrowser.Toolbar">
            <reference ref="Vcs.RepositoryChangesBrowserToolbar"/>
            <separator/>
            <action id="GitReflog.DiffMode"
                    class="cz.loplex.reflog.actions.GitReflogDiffModeSwitch"/>
        </group>

        <group id="GitReflog.ChangesBrowser.Popup">
            <reference ref="Vcs.RepositoryChangesBrowserMenu"/>
            <separator/>
            <group id="GitReflog.DiffModes"
                   class="cz.loplex.reflog.actions.GitReflogDiffModeGroup"/>
        </group>

        <group id="GitReflog.ContextMenu">
            <action id="GitReflog.ShowDiff"
                    class="cz.loplex.reflog.actions.GitReflogShowDiffAction"
                    icon="AllIcons.Actions.Diff"
                    use-shortcut-of="Diff.ShowDiff"/>
            <!-- Also here, not only on the file pane: the entries being compared are selected in this table, so
                 this is where the question is asked. -->
            <reference ref="GitReflog.DiffModes"/>
            <separator/>
            <action id="GitReflog.CheckoutRevision"
                    class="cz.loplex.reflog.actions.GitReflogCheckoutRevisionAction"/>
            <action id="GitReflog.NewBranch"
                    class="cz.loplex.reflog.actions.GitReflogNewBranchAction"
                    icon="AllIcons.Vcs.BranchNode"/>
            <action id="GitReflog.Reset"
                    class="cz.loplex.reflog.actions.GitReflogResetAction"
                    icon="AllIcons.Vcs.Merge"/>
            <separator/>
            <!-- Both platform actions work off the revision numbers the tab publishes into the data context. -->
            <reference ref="Vcs.Log.SelectInLog"/>
            <reference ref="Vcs.CopyRevisionNumberAction"/>
        </group>
    </actions>

</idea-plugin>
